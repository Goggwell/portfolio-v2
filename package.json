{
  "name": "portfolio_2.0",
  "version": "1.0.0",
  "description": "Updated version of my portfolio, built with Gatsby and React",
  "main": "index.js",
  "author": "Samuel Yusuf <zulkarnaensamuel1@gmail.com>",
  "license": "MIT",
  "scripts": {
    "compile": "gulp style",
    "watch": "gulp watch",
    "dev": "nodemon server.js",
    "start:node": "node server.js",
    "build:parcel": "parcel build src/public/index.html --no-source-maps --public-url ./ --no-cache",
    "packagejs": "parcel build src/public/scripts/colorhover.js --no-source-maps --no-cache",
    "develop:parcel": "parcel build src/public/index.html --no-source-maps --no-minify --public-url ./ --no-cache",
    "build:parcel:nominify": "parcel build src/public/index.html --no-source-maps --public-url ./ --no-cache --no-minify"
  },
  "devDependencies": {
    "browser-sync": "^2.27.5",
    "gulp": "^4.0.2",
    "gulp-sass": "^5.0.0",
    "nodemon": "^2.0.12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "gulp-cli": "^2.3.0",
    "multiparty": "^4.2.2",
    "node-sass": "^6.0.1",
    "nodemailer": "^6.6.3",
    "parcel-bundler": "^1.12.5",
    "sass": "^1.39.0"
  },
  "engines": {
    "node": "14.x"
  }
}
